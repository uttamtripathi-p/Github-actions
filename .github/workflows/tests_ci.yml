name: real tests
on:
  push:
    branches: [main]

jobs:
  running_script:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: running the code
        run: |
          ./disk_check.sh

