name: real tests
on:
  push:
    branches: [main]

jobs:
  running_script:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Running the code
        run: |
          chmod +x ./disk_check.sh
          ./disk_check.sh
          cat disk_check.sh

